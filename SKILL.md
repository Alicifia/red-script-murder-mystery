---
name: red-script-murder-mystery
description: 从红色历史题材创作完整剧本杀（玩家本/DM本/卡牌/物料）的 15 步全流程：检索扩词与广域搜索（小众事件优先地方志/校史来源）、知识库提取去重、切入点候选、三幕大纲、多方案机制、创作蓝本与角色信息边界表、视角限定玩家本、DM 手册、五类卡牌、史实/泄漏/时间线三件套质检与定点修复、物料清单与封面海报绘制（支持直接绘图则代绘，否则输出提示词清单给用户自绘）、分类打包与案例提炼。当用户想以红色历史/党史/校史事件为题材创作剧本杀、沉浸式团建剧本或红色主题桌面游戏时使用。
---

# 红色剧本杀创作工作流

> © 2026 连理拾光工作室。保留所有权利。  
> 本技能采用 CC BY-NC-SA 4.0 许可：可自由转载与修改，须署名、禁止商业用途、衍生作品须以相同方式共享。详见仓库 LICENSE。

从红色历史题材到可开团的完整剧本杀成品（玩家本 / DM 本 / 卡牌 / 物料）的 15 步创作流程。已在真实红色剧本杀案例上完整验证（案例内容从略，请用 `references/cases-template.md` 自建案例库）。

## 前置：确认能力与环境

开始前向用户确认三件事（有缺项就降级处理，不要中止流程）：

1. **搜索能力**：优先用 agent 自带的联网搜索。没有自带搜索时，让用户提供 Tavily 或 SerpAPI 的 key。
2. **LLM**：直接用当前对话模型完成各阶段生成，无需额外配置。用户要求接外部 OpenAI 兼容接口时才改用 API。
3. **绘图**：见「步骤 13 · 物料绘制」的分流逻辑。

项目目录约定：为每个题材建一个工作文件夹（如 `题材名-redscript/`），所有产物按阶段分目录保存（`knowledge/`、`drafts/`、`qc/`、`materials/`、`pack/`）。

## 步骤 0 · 开场引导（收集创作输入）

触发后不要直接开跑，先一次性向用户收集以下输入（用一段话加清单提问；用户给多少算多少，缺的用默认值，不要反复追问）：

1. **题材主题（必填）**：一个红色历史事件 / 时期 / 人物 / 地点，一句话即可。用户没给就先问，拿到主题再进步骤 1；用户只有模糊想法（如「我们学校的校史」）时，先用 1–2 轮搜索帮用户把题材具体化，确认后再继续。
2. **补充提示 hints（选填）**：想突出的角度、必须避开的元素、目标玩家与场景（学生团建 / 党建活动 / 实景展厅）。
3. **人数（选填，默认 6 人本）**：影响大纲、玩家本份数、名牌与打印数量等下游产物。
4. **自建案例库（选填）**：有没有按 `references/cases-template.md` 整理的以往剧本条目，有则 `{cases}` 引用，没有填「无」。
5. **输出目录（选填）**：默认在当前工作区建 `题材名-redscript/`。

确认主题后建好项目文件夹再进步骤 1。

## 总流程（15 步，每步完成经用户确认再进下一步）

| 步 | 任务 | 提示词 / 参考 | 产物 |
|---|---|---|---|
| 1 | 检索词扩写 | `references/prompts/broaden.md` | 4–8 条检索词 |
| 2 | 广域搜索 | 用上一步检索词执行搜索 | 原始检索结果 |
| 3 | 知识提取去重 | `references/prompts/extract.md`，多批次合并去重 | 结构化知识库（JSON） |
| 4 | 切入点候选 | `references/prompts/angle.md`，默认 6 人本 | 4 个切入点供用户挑选 |
| 5 | 剧本大纲 | `references/prompts/outline.md` | Markdown 大纲 |
| 6 | 机制多方案 | `references/prompts/mechanics.md`，每环节 ≥2 方案供用户选 | 机制方案 JSON |
| 7 | 创作蓝本 | `references/prompts/blueprint.md`，产出 roleKnowledgeMap（后续质检白名单） | 蓝本 + 信息边界表 |
| 8 | 玩家本 ×N | `references/prompts/draft-player.md`，逐角色生成，严守视角限定 | 每个角色一份 Markdown |
| 9 | DM 本 | `references/prompts/draft-dm.md` | 8 节结构 DM 手册 |
| 10 | 卡牌文字稿 | `references/prompts/draft-cards.md` | 五类卡牌 + 投票纸 |
| 11 | 质检三件套 | `references/prompts/qc-history.md`、`qc-leak.md`（逐角色）、`qc-timeline.md` | issues 清单 |
| 12 | 定点修复 | `references/prompts/qc-fix.md`，只重写受影响段落；error 修完复检，循环至无 error | 修订稿 |
| 13 | 物料清单 + 绘制 | `references/prompts/matlist.md` + 本文件「物料绘制」节 | 物料文件 |
| 14 | 打包与案例提炼 | 见「打包与案例提炼」节 | zip + 案例 md |

说明：步骤 11–12 与用户共同修改共用同一套机制（质检 → qc-fix 定点修复），用户任何阶段的修改意见都走定点修复，不整本重写。步骤 8/9/10 的骨架硬性要求已内嵌在各提示词中，结构依据见 `references/structure-template.md`；`{cases}` 变量用你自建案例库的条目摘录（格式见 `references/cases-template.md`），只借鉴思路不抄袭情节；没有案例库时 `{cases}` 填「无」。

## 用户确认点（每步与用户交互什么）

每步结束都要把产物呈现给用户、拿到明确确认或选择后再进下一步。各步交互要点：

| 步 | 呈现什么 | 用户决策 |
|---|---|---|
| 1 | 检索词清单 | 确认或补充角度后再搜 |
| 3 | 知识库条目（标题 + 摘要 + 来源数） | 确认史实锚点是否足够；不足则回步骤 1 补检索词再搜一轮 |
| 4 | 4 个切入点（标题/视角/冲突分/钩子） | 选一个，或要求重新生成 |
| 5 | 大纲全文 | 确认或提出修改（修改走 qc-fix 定点修复） |
| 6 | 每环节 ≥2 方案的利弊对比 | 逐环节选定方案，或要求新方案 |
| 7 | 蓝本 + roleKnowledgeMap | 确认信息边界表（它是后续视角质检的白名单，务必请用户细看） |
| 8–10 | 玩家本 / DM 本 / 卡牌稿 | 分批过目，收集修改意见（走 qc-fix） |
| 11–12 | issues 清单（error 在前、warning 在后） | error 必修；warning 由用户裁决改不改；修完简述改动点并复检 |
| 13 | 物料清单 + 成套物料样张 | 确认清单与样张风格后才批量生成 |
| 14 | 打包结果 + 自检清单核对情况 | 确认后交付 |

## 搜索策略（步骤 1–3 的执行要点）

- 市/县/学校级小众事件：至少 2 条检索词带地方来源倾向词（「党史」「地方志」「校史」「纪念馆」「县志」），优先命中地方官网、党史网、校史馆。
- extract 严格抽有史实依据的内容，confidence 标 medium/low；宁可少不要凑数。
- 知识库是全流程史实锚点的唯一来源，后续大纲、质检都以它对齐。

## 物料绘制（步骤 13，分流逻辑）

matlist.md 会把物料分成两类（`method` 字段）：

1. **svg 类**（名牌、贴纸、票证、卡牌等版式明确的物料）：用代码绘制（HTML/SVG 导出 PNG/PDF），零绘图额度。
2. **image-api 类**（套盒封面、玩家本封面、DM 本封面、地图、海报）：matlist 已附每件的 60 字内中文绘图提示词，分流规则：

   - **当前 agent 支持直接绘图**（有 image_generation 之类的工具/插件）：直接用保存好的 `prompt` 逐件生成；成套物料（如 6 角色封面）**先出一件样张给用户确认风格，确认后再批量**。
   - **不支持直接绘图**：把全部 image-api 物料的提示词整理成一张《绘图提示词清单》（物料名 / 尺寸 / 提示词三列），输出给用户，让用户自行拿到绘图 AI（智谱 cogview、即梦、Midjourney 等）生成，生成后放回 `materials/` 继续打包。

额度敏感：批量出图前先向用户确认件数与预计消耗。

## 打包与案例提炼（步骤 14）

- 按物料清单分类打包：文本打印类（玩家本/DM本/卡牌稿 md）、卡牌类、道具类、封面海报类，压缩为 `《剧本名》物料包.zip`。
- 同时提炼一份案例 md（切入点思路、机制取舍、物料清单、踩坑记录），按 `references/cases-template.md` 的格式追加到你的案例库。

## 红色题材铁律（每步自检）

1. 史实锚点必须与知识库一致；DM 本必须有「史实边界标注」节（哪些史实、哪些虚构）。
2. 虚构不得与锚定史实冲突；敏感时代背景中对手设定为模糊群体，避免影射。
3. 角色本只写该角色可知信息（roleKnowledgeMap 白名单），真相页翻页限制最严。
4. 价值导向有点题设计（口号背诵、社论/独白式升华话术），在 DM 本单独标注。

## 交付自检清单（定稿前逐项核对）

1. 玩家本每页有翻页限制行；秘密信息仅出现在持有者本中。
2. 三幕资源贯通、有兜底机制防卡死。
3. DM 本含：分环节时长、控时/扶车话术、真相保密纪律、结局触发矩阵、史实边界标注。
4. 卡牌覆盖线索（含真假）/任务/道具/投票/成就五类。
5. 物料清单可直接转打印统计表（数量/尺寸/材质/一次性标注齐全）。
