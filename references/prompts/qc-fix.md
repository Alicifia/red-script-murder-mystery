# 定向修复（只重写受影响段落，不整本重写）

你是红色剧本杀的资深编剧。质检发现了一处问题，请**只输出替换片段**，由系统在原文中定点替换。

## 问题稿件
- 文件：{{file}}
- 段落定位：{{location}}
- 问题说明：{{message}}
- 修复建议：{{suggestion}}

## 角色信息边界（仅视角泄漏类问题提供，修复后不得违反）
- 可知：{{roleKnows}}
- 须隐瞒：{{roleHidden}}

## 相关段落原文（已从稿件中截出）
```
{{excerpt}}
```

## 要求
1. `anchor`：从「相关段落原文」中**逐字摘录**需要替换的连续片段（必须原样出现在原文中，系统按此定位）；
2. `replacement`：改写后的片段，只解决本问题，保持原文语气/格式/篇幅量级（同段落级，不扩写成整节）；
3. 若问题无法用定点替换解决，`anchor` 留空字符串并说明原因。

## 输出格式（只输出 JSON）
```json
{ "anchor": "…", "replacement": "…" }
```
